# Optional support

Whyline is **MIT licensed**. No paywall. No account required.

If you find it useful, voluntary tips are welcome on **Solana (SOL only)**:

```
B4ZFerkb7DctTLabgzh3F8aGhJF7EVDxcnLz6e2zqZwb
```

To show the in-app tip button on your own deployment, set in `.env`:

```env
SOL_DONATION_ADDRESS=your_solana_address
```

Leave empty (default) to disable donations entirely — no button, no prompts.